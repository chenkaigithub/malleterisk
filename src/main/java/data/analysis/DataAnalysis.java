package data.analysis;

public class DataAnalysis {
	/*
		- document distribution per class (histogram) - just fucking do it 
		- analyze(InstanceList): # instances, # classes, # avg instances/class, # largest class, 
		# smallest class, # time period for classes, # participants in classes 
		- conditional probability given the length of the messages 
		- time distribution of messages 
		- number of instances of each class used in train and in test (this already exists.. it’s 
		used in the confusion matrix, etc) 
	 */
}
